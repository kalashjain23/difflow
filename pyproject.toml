[project]
name = "difflow"
version = "0.1.0"
description = "library for diffusion and flow matching models"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "einops>=0.8.2",
    "torch>=2.8.0",
    "torchvision>=0.23.0",
]
